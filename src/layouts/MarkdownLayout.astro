---
import BaseLayout from "./BaseLayout.astro";

const { title, menu : menuKey } = (Astro.props.frontmatter || Astro.props);

---

<BaseLayout {menuKey} {title} markdown={true}>
    <slot />
</BaseLayout>